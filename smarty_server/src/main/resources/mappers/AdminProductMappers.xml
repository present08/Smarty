<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.smarty.mapper.AdminProductMapper">
    <insert id="register" parameterType="com.green.smarty.dto.ProductAdminDTO">
        insert into product (product_id, facility_id, product_name, stock, price, product_images)
        values (#{product_id}, #{facility_id}, #{product_name}, #{stock}, #{price}, #{product_images})
    </insert>

    <insert id="fileUpload">
        insert into product_attach (product_id, origin_path, thumbnail_path, file_name)
        values (#{product_id}, #{origin_path}, #{thumbnail_path}, #{file_name})
    </insert>

    <select id="getList" resultType="com.green.smarty.dto.ProductAdminDTO">
        select * from product
    </select>
    <select id="read" resultType="com.green.smarty.dto.ProductAdminDTO">
        select * from product where product_id=#{product_id}
    </select>

</mapper>

