<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.smarty.mapper.FacilityMapper">
    <!--sql 구문을 써서 db에서 불러온다-->
<!--    <insert id="register" parameterType="com.green.smarty.vo.FacilityVO" useGeneratedKeys="true" keyProperty="facility_id">-->
<!--        INSERT INTO facility_tbl (-->
<!--            facility_name,-->
<!--            price,-->
<!--            day_rate,-->
<!--            night_rate,-->
<!--            open_time,-->
<!--            close_time-->
<!--        )-->
<!--        values(-->
<!--            #{facility_name},-->
<!--            #{price},-->
<!--            #{day_rate},-->
<!--            #{night_rate},-->
<!--            #{open_time},-->
<!--            #{close_time}-->
<!--        )-->
<!--    </insert>-->

    <select id="getAllFacility" resultType="com.green.smarty.vo.FacilityVO">
        select * from facility
    </select>

    <update id="updateFacility" parameterType="com.green.smarty.vo.FacilityVO">
        update facility set facility_name = #{facility_name} where facility_id = #{facility_id}
    </update>

    <select id="getFacilityName" resultType="com.green.smarty.vo.FacilityVO">
        SELECT
            facility_id,
            facility_name,
            price,
            day_rate,
            night_rate,
            open_time,
            close_time
        FROM
            facility
    </select>

    <insert id="insertFacility" parameterType="com.green.smarty.vo.FacilityVO">
        insert into facility(facility_id , facility_name, open_time, close_time, default_time,basic_fee,rate_adjustment,hot_time,contact, info, caution, court,product, facility_status, facility_images)
        values(#{facility_id}, #{facility_name}, #{open_time}, #{close_time}, #{default_time}, #{basic_fee}, #{rate_adjustment}, #{hot_time},#{contact}, #{info}, #{caution}, #{court}, #{product}, #{facility_status}, #{facility_images})
    </insert>

</mapper>