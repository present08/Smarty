<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.smarty.mapper.ProductMapper">
    <insert id="register" parameterType="ProductDTO" useGeneratedKeys="true" keyProperty="product_id">
        insert into product (facility_id, product_name, price, quantity, product_images)
        values (#{facility_id}, #{product_name}, #{price}, #{quantity}, #{product_images})
    </insert>
    <insert id="fileUpload">
        insert into product_attach (product_id, origin_path, thumbnail_path, file_name)
        values (#{product_id}, #{origin_path}, #{thumbnail_path}, #{file_name})
    </insert>

    <select id="getList" resultType="ProductDTO">
        select * from product
    </select>
    <select id="read" resultType="ProductDTO">
        select * from product where product_id=#{product_id}
    </select>

</mapper>
